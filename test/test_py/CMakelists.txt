cmake_minimum_required(VERSION 3.0.0)
project(flat3 VERSION 0.1.0)

include(CTest)
enable_testing()

add_executable(gtestprogram_py)
target_compile_features(gtestprogram_py PRIVATE cxx_std_20)

target_sources(gtestprogram_py PRIVATE 
    test.cpp 
    )

target_include_directories(gtestprogram_py PUBLIC 
    ${CMAKE_CURRENT_LIST_DIR}/../../3rdparty/pybind11/include
    )

set(CPACK_PROJECT_NAME ${PROJECT_NAME})
set(CPACK_PROJECT_VERSION ${PROJECT_VERSION})
include(CPack)
